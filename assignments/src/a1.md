---
title: Analytics with Apache Spark and DuckDB
kicker: Assignment 1
due: Fri May 29, 11:59 PM ET
---

## FAQs

* **PLEASE DO NOT PROCRASTINATE!!** If you wait till the last minute to work on 
your assignment and ask for help, no one will be able to help you.
These assignments are not hard, but some steps might take longer than
you expect, esp. when you are new to it. So, start working on it
**EARLY**.



## Overview

This assignment is designed to support your in-class understanding of
how big data analytics stacks work and get some hands-on experience
in using them. You will need to deploy Apache Spark and DuckDB and 
implement a small graph analytics application (PageRank) on them using
OpenCode. You will produce a short report detailing your observations
and takeaways.



## Part 0: Environment setup

You will complete this assignment in AWS Academy.  

In this assignment, you should create one EC2 VM instance of type
`t3.large`.  Spark uses Java. So, as the first step, you
should run the following commands on every EC2 instance to install
Java. Ubuntu 24.04 LTS comes with `python3` (`Python 3.12.3`)
installed. So you don't need to manually install python yourself.


```bash
$ sudo apt update
$ sudo apt install -y openjdk-8-jdk
```



## Part 1: Software deployment

### Apache Spark

[Apache Spark](https://spark.apache.org/docs/latest/index.html) is a 
powerful open-source analytics engine for big data processing.
Spark is built on its core idea of [Resilient Distributed Datasets (RDDs)](https://www.usenix.org/system/files/conference/nsdi12/nsdi12-final138.pdf). Spark standalone
consists of a set of daemons: a `Master` daemon, and a set of
`Worker` daemons. Spark applications are coordinated by a
`SparkContext` object, which will connect to the `Master`, which is
responsible for allocating resources across applications. Once
connected, Spark acquires `Executors` on every `Worker` node in the
cluster, which are processes that run computations and store data for
your applications. Finally, the application's tasks are handed to
`Executors` for execution. We will use Spark in standalone mode,
which means that it does not need to rely on resource management
systems like YARN.

Instructions on deploying a Spark cluster can be found in 
Spark's [official document](https://spark.apache.org/docs/latest/spark-standalone.html).
For this assignment, you don't need to build a standalone Spark.
Instead, use AI to install **PySpark** for you.


## Part 2: PageRank

In this part, you will implement the
[PageRank algorithm](http://infolab.stanford.edu/~backrub/google.html) (§2.1)
(or [the Wikipedia version](https://en.wikipedia.org/wiki/PageRank#Simplified_algorithm)),
which is an algorithm used by search engines like Google to evaluate
the quality of links to a webpage. The algorithm can be summarized as
follows:

1. Set initial rank of each page to be 1.
2. On each iteration, each page p contributes to its outgoing neighbors a value of rank(p)/(# of outgoing neighbors of p).
3. Update each page's rank to be 0.15 + 0.85 * (sum of contributions).
4. Go to next iteration.

In this assignment, we will run the algorithm on the.
[`Berkeley-Stanford` web graph dataset](https://snap.stanford.edu/data/web-BerkStan.html).
Each line in the dataset consists of a page and one of its neighbors.
Ask the AI agent to decompress the downloaded `.gz` dataset. 

If you do it manually, you should use the following commands:

```sh
$ wget https://snap.stanford.edu/data/web-BerkStan.txt.gz
$ gunzip web-BerkStan.txt.gz
```

**In this part, always run the algorithm for a total of 10
iterations.** However, for debugging purpose, you may use a smaller
iteration number, say 3 or 4 so that you don't need to wait for too
long to see the result. 

Your algorithm should **initialize ranks only from pages that have
outgoing links and only keep pages that receive contributions in each iteration**. 

This part features four tasks as follows:

* **Task 1.** Write a PySpark application that implements the 
PageRank algorithm. Your code should use both the two CPU cores
on your VM. 
Your PageRank application should output the following
two results: 
1. Print the first 50 rows with the highest ranks;
2. Save the computed results as a csv file;
3. Save the measured time result as a separate log file. 

* **Task 2.** Implement the same PageRank algorithm, but with 
DuckDB. Use this database-based implementation as a way
to verify the result from Task 1. You should use the AI agent to: 
1. Install DuckDB on your VM;
2. Launch the DuckDB instance;
3. Load the dataset into it;
4. Use SQL to implement the iterative PageRank algorithm; 
5. Measure the time it takes for the DuckDB version to complete 10 iterations;
6. Save the first 50 rows as a csv file;
7. Save the measured time result as a separate log file. 

* **Task 3.** Write a Spark program that uses its built-in PageRank 
lbirary to verify the results. You should ask the AI agent to switch 
to Spark's [GraphFrames](https://graphframes.io/) framework, 
which expose a 
[PageRank API](https://graphframes.io/api/python/graphframes.html#graphframes.GraphFrame.pageRank). 
Make sure that the AI agent implements the same PageRank algorithm
as you did before with Task 1 and 2. 
1. Measure the time it takes for Spark GraphFrames to complete 10 iterations;
2. Save the first 50 rows as a csv file;
3. Save the measured time result as a separate log file; 
4. Save the entire session transcript as a markdown file: this
transcript markdown file should contain all your interactive
conversation history covering Task 1-3.

* **Task 4.** Prepare a report by asking your AI agent to organize
all responses from this session into a single HTML file. The report
should let you visually review the exploration process, including
the key results from the three PageRank implementations shown side
by side. Present or reason about the differences in performance or your 
own findings, and ask the AI agent to include your reasoning in the 
generated HTML. 
The HTML idea is drawn from
[this X from Claude Code](https://x.com/trq212/status/2052809885763747935?s=20).


## Deliverables

You should submit a `tar.gz` file to Canvas, which follows the naming
convention of `LastName_FirstName_ComputingID_A1.tar.gz`. The
submitted file should include:
* the source-code files containing the code of each task (each file should be meaningfully named
so that the teaching staff can know which file is for which task); 
* the opencode session transcript markdown file that records all your conversations
with AI, including model's thinking, tool details, and assistant metadata; 
* the **HTML report** co-authored by both you and your AI agent.



